from setuptools import setup, find_packages

setup(
    name="saytoai-shared",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    author="SayToAI",
    description="Shared utilities for SayToAI applications",
    python_requires=">=3.8",
) 